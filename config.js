/* config.js — the ONE place for this firm's Apps Script URL.
   Read by all six client pages (upload, gatherinfo, findoc, feedback, review,
   privacy-policy). Paste the /exec URL from Apps Script → Deploy → Manage
   deployments below. Keep this file in the SAME folder as crm.html and the pages. */
var SCRIPT_URL = 'PASTE_YOUR_APPS_SCRIPT_EXEC_URL_HERE';
